@model IEnumerable<WebApplication1.Models.UnidadeDeMedida>

@{
    ViewBag.Title = "Index";
}

<h2>Unidades De Medida</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sigla)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fracionavel)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sigla)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fracionavel)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.UnidadeDeMedidaID }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.UnidadeDeMedidaID }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.UnidadeDeMedidaID })
        </td>
    </tr>
}

</table>

@model IEnumerable<WebApplication1.Models.UnidadeDeMedida>

@{
    ViewBag.Title = "Index";
}

<h2>Unidades De Medida</h2>

<p>
    <a href="#" class="btnCreate">Nova Unidade de Medida</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UnidadeDeMedidaID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sigla)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fracionavel)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UnidadeDeMedidaID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sigla)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fracionavel)
            </td>
            <td>
                <a href="#" class="btnEdit" data-value="@item.UnidadeDeMedidaID">Editar</a> |
                <a href="#" class="btnDetails" data-value="@item.UnidadeDeMedidaID">Detalhes</a> |
                <a href="#" class="btnDelete" data-value="@item.UnidadeDeMedidaID">Excluir</a>
            </td>
        </tr>
    }

</table>

<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-body">
                <div class="modal-header">
                    <div id="conteudoModal">
                    </div>
                </div>
                <a href="#" class="btnVoltar">Voltar</a>
            </div>
        </div>
    </div>
</div>

@section scripts{
    <script type="text/javascript">

        $(document).ready(function () {

            $(".btnCreate").click(function () {

                var id = $(this).data("value");

                $("#conteudoModal").load("/UnidadesDeMedida/Create/",
                    function () {
                        $('#myModal').modal("show");
                    });
            });
        });

        $(document).ready(function () {

            $(".btnDetails").click(function () {

                var id = $(this).data("value");

                $("#conteudoModal").load("/UnidadesDeMedida/Details/" + id,
                    function () {
                        $('#myModal').modal("show");
                    });
            });
        });

        $(document).ready(function () {

            $(".btnDelete").click(function () {

                var id = $(this).data("value");

                $("#conteudoModal").load("/UnidadesDeMedida/Delete/" + id,
                    function () {
                        $('#myModal').modal("show");
                    });
            });
        });

        $(document).ready(function () {

            $(".btnEdit").click(function () {

                var id = $(this).data("value");

                $("#conteudoModal").load("/UnidadesDeMedida/Edit/" + id,
                    function () {
                        $('#myModal').modal('show');
                    });
            });
        });

        $(document).ready(function () {
            $(".btnVoltar").click(function () {
                $('#myModal').modal("hide");
                window.location.reload();
            })
        })
    </script>
}
